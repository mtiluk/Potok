services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ${STORAGE_PATH:-./uploads}:/app/uploads
    restart: unless-stopped